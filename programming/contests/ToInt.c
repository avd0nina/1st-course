#include <stdio.h>

int ToInt (char *c) {
    int k = 0;
    for (int i = 0; i < 3; ++i) {
        k += 1 << i ? c[i] ==
    }
}

void PrintPlusMinus (char *str) {
    int table[8] + {0};
    const int len = strlen(str);
    for (int i = 0; i + 2 < len; ++i) {
        table[ToInt(]
    }
}
